# パートナー発信物ジェネレーター — 引き継ぎメモ（CLAUDE.md）

このリポジトリは、パートナー締結時の発信物（メール・SNS・HPニュース）を、団体ごとに変わる部分だけ入力して一括生成するWebツール。
画面デザインは v0 で作成し、Claude Code で HTML+JS（方式B）に移植・実装した。

設計の詳細な経緯は仕様書（claude.ai の Docs）にある。本ファイルは実装に必要な決定事項をすべて含む。

---

## 0. 決定済み事項・現在の構成

### セキュリティ方式：B（HTML+JS ＋ StatiCrypt）に決定済み

- v0 の出力は Next.js だったが、ユーザー判断で **方式B** を採用。Next.js は撤去済み
- Preact（約10KB）＋ esbuild で JSX を1本の JS にバンドルし、**CSS・JS・設定をすべて `dist/index.html` 1枚にインライン化**する
  - StatiCrypt は HTML しか暗号化しないため、**JS を別ファイルに出してはいけない**（デプロイ時に `dist/*.js` があれば失敗させている）
- GAS の URL と利用者キーはビルド時に Secrets（`GAS_URL` / `GAS_KEY`）から `window.APP_CONFIG` として埋め込み、StatiCrypt（`SITE_PASSWORD`）で暗号化
- 管理者パスワードはフロントに埋め込まない。画面で入力し sessionStorage に保持、GAS 側でも必ず照合
- デプロイ後の出力に平文の URL・キーが含まれていないかをワークフローで検査している

### 実データの置き場所

- 実データはリポジトリに置かない。告知文面・団体・設定はスプレッドシート、契約書・申込書の雛形は Google ドキュメント、告知画像の雛形は Google スライド、団体ロゴは画像フォルダ（ドライブ）にある
- Orbit 契約書・申込書の原本 docx、v0 の旧画面コードと旧文面は移行済みのためリポジトリから削除した（Git の履歴には残っている）
- モックデータ（`gas/MockData.gs`）は架空の情報のみ

### ディレクトリ

```
src/                フロント（Preact + Tailwind v4）
  main.jsx          画面の外枠・ナビ・管理者パス入力・トースト
  lib/engine.js     テンプレエンジン（差し込み・日付書式・ランク別選択・X分割・X文字数）
  lib/api.js        GAS クライアント（text/plain POST、キー・管理者パス付与、エラー文言）
  lib/mock-backend.js  モックモード（gas/Core.gs をブラウザ内で実行）
  lib/memory-db.js  SheetDb と同じ形のメモリDB（モックとテストで使用）
  views/            Home / Create（生成フロー1〜3）/ Output（4 出力）/ History / Admin / TemplateEditor
gas/                GAS（clasp でそのまま push できる構成）
  Schema.gs         シート定義（ヘッダー名 ↔ APIのプロパティ名）
  Core.gs           doPost の中身。GAS API に依存しない（env 経由で DB を受け取る）
  SheetDb.gs        スプレッドシート読み書き
  Main.gs           doPost / doGet / setup / スクリプトプロパティ
  MockData.gs       setup() で投入するモックデータ
test/               node:test（engine と GAS Core）
scripts/build.mjs   ビルド・開発サーバー
```

### コマンド

- `npm run dev` … http://localhost:5173 。`config.local.json`（`{"gasUrl": "...", "key": "..."}`、git 管理外）が無ければモックモード（管理者パスは `admin`）
- `npm test` … テンプレエンジンと GAS Core の単体テスト
- `npm run build` … 本番ビルド（`GAS_URL` / `GAS_KEY` 必須。`ALLOW_MOCK=1` でモックモード）
- `npm run encrypt` … `STATICRYPT_PASSWORD` で `dist/*.html` を暗号化

---

## 1. 概要

- **目的**：個別に資料を作る手間を減らし、完成度の高い文面を安定して作る
- **初期対象の媒体**：メール、X、Instagram、note、HPニュース
- **書類**：契約書・申込書（Google ドキュメントの雛形に差し込んで PDF / Docx で出力）
- **告知画像**：Google スライドの雛形に差し込み、スライドごとに PNG で出力（団体ロゴも入る）
- **使い方**：団体を選ぶ → セットとランクを選ぶ → 案件情報を入力 → 媒体ごとの文面をコピー

## 2. 構成

```
GitHub Pages（StatiCrypt で暗号化した画面） ──キー付きリクエスト──> GAS Webアプリ ──> スプレッドシート（非公開）
                                                                     └──> Googleドライブ（書類・告知画像の雛形、団体ロゴ）
```

- データはすべて GAS 経由。**公開CSVは使わない**（団体マスタに連絡先が入るため）
- スプシ・ドライブは管理メンバーのみ共有。GAS は所有者権限で実行（「自分として実行」「全員アクセス可」でデプロイ）
- ダッシュボードとは別リポジトリ
- ログイン直後に必要データを一括取得（`init`）して画面側で保持。以降の画面遷移は通信なし（履歴の一覧・詳細と書き込みのみ通信）
- 最新化：ページ移動のたび・別タブから戻ったとき（30秒以上空いたら）に裏で `init` と履歴一覧を取り直す。ヘッダーの「最新に更新」で手動でも可。読み込みが重なったら最後に始めたものだけ反映する
- `doGet` は `USE_POST` エラーを返す。画面は `init` の応答に一覧が揃っていなければ同じエラーにする（POST が転送で GET に変わった場合。Google に複数アカウントでログインしていると起きやすい）

### GAS 通信の注意

- GitHub Pages → GAS の POST は CORS のプリフライトを避けるため `Content-Type: text/plain` で JSON 文字列を送る
- GAS は `doPost(e)` で `JSON.parse(e.postData.contents)` し、`ContentService.createTextOutput(JSON.stringify(res)).setMimeType(ContentService.MimeType.JSON)` で返す
- リクエスト形式：`{ action: "init", key: "...", adminPass?: "...", payload: {...} }`
- レスポンス形式：`{ ok: true, data: {...} }` / `{ ok: false, error: "UNAUTHORIZED", message?: "..." }`

## 3. 権限

ルール：**追加と出力は利用者、既存の変更・削除は管理者。**

| 操作 | 利用者 | 管理者 |
| --- | --- | --- |
| データ一括読み込み | ○ | ○ |
| 資料の生成・出力・履歴保存 | ○ | ○ |
| 履歴の閲覧・作り直し | ○ | ○ |
| 新規追加（テンプレ、セット、媒体、項目、ランク、日付書式、団体、共通設定） | ○ | ○ |
| 既存データの編集 | × | ○ |
| 既存データの無効化 | × | ○ |
| 団体マスタの更新（生成フロー中の修正反映） | × | ○ |
| 履歴の削除 | × | ○ |

- 利用者が追加したテンプレも、以後の修正は管理者のみ（例外なし・確定）
- 管理者パスは、編集・無効化ボタン押下時に入力。画面が保持し該当リクエストに自動付与。**GAS 側でも必ず照合**（画面側だけの制御にしない）
- 書き込み系は GAS で `LockService` による排他制御
- スクリプトプロパティ：`USER_KEY`（利用者キー）、`ADMIN_PASSWORD`、`SPREADSHEET_ID`（スプレッドシートに紐づくスクリプトなら不要）、`OUTPUT_FOLDER_ID`（書類・告知画像の控えを残すフォルダ。省略時は控えを残さない）、`LOGO_FOLDER_ID`（団体ロゴなどの画像の保存先。一般公開しない）

## 4. 画面

```
ログイン（StatiCrypt） → ホーム ┬ 資料を作る：1 団体選択 → 2 セット・ランク選択 → 3 案件情報入力 → 4 出力
                               ├ 履歴（→「これを元に作り直す」で 3 へ）
                               └ 管理（テンプレート／セット／媒体／入力項目／ランク／日付書式／団体マスタ／共通設定）
```

### 生成フロー
1. **団体選択**：既存団体を検索して選択。新規はその場で入力しマスタに追加（利用者可）
2. **セット・ランク選択**：例「締結告知セット」＋「ゴールド」。含まれる媒体がチェック付きで並び、不要なものは外せる
3. **案件情報入力**
   - 選んだテンプレで使われている項目だけをフォーム表示（テンプレ本文から `{{ }}` を抽出して生成）
   - 団体情報はマスタから自動入力。修正は今回の資料のみに反映（管理者はマスタ更新も選択可）
   - 右側にリアルタイムプレビュー
   - 作成者名：初回のみ入力し localStorage に記憶（本人確認ではなく記録用）
4. **出力**
   - 媒体ごとのタブ、欄ごと（件名／本文／ハッシュタグ等）にコピーボタン
   - X はスレッド分割し、投稿ごとに文字数とコピーボタン
   - 画面上で文面を直接修正可（テンプレには影響しない）
   - 生成時に履歴へ自動保存。修正後の文面で上書き保存

### 履歴
- 日時・団体・セットで一覧、検索
- 開くと出力画面を再表示
- 「これを元に作り直す」で入力値を引き継いで再生成（新しい履歴として保存）

### 管理画面
- 利用者でも入れて、一覧閲覧と新規追加が可能
- 編集・無効化ボタン押下時に管理者パスを要求（入力後はそのタブを閉じるまで再入力なし）
- 削除は「有効」列を FALSE にする無効化。過去の履歴は壊さない

### テンプレート編集画面
- 左：本文エディタ。項目一覧をクリックで `{{項目名}}` を挿入
- 日付項目をクリック → 書式候補 → 選ぶと `{{締結日:M/D(曜)}}` を挿入。挿入済みのものをクリックで書式変更
- 「X区切り」挿入ボタン
- 右：モックデータ（入力項目の「入力例」）でのプレビュー。X は分割結果と文字数も表示
- 未登録の項目名を書くと保存時に「新しい項目として登録しますか？」と確認
- セット・媒体・ランクの選択肢の「＋ 新しく作る…」から、その場で新規追加できる（追加なので利用者も可）。媒体は欄の構成をひな形（本文だけ／件名＋本文／タイトル＋本文／本文＋ハッシュタグ／X）から選ぶ。書類・告知画像では「本文」欄ひとつ
- 書類テンプレ（PDF / Docx）・告知画像テンプレ（画像）は本文を画面で編集しない。「Googleドキュメント／スライドで開く」（編集画面・一覧）で雛形を直し、「読み込む」→ 保存で反映する

## 5. テンプレート仕様

| 記法 | 意味 | 出力例 |
| --- | --- | --- |
| `{{団体名}}` | 入力項目・団体情報の差し込み | サンプル団体 |
| `{{署名}}` | 共通設定の差し込み | サンプル運営事務局 |
| `{{締結日}}` | 日付（項目設定の標準書式） | 2026年9月24日 |
| `{{締結日:M/D(曜)}}` | 日付（書式を上書き） | 9/24(木) |
| `---`（単独行） | X投稿の区切り | 次の投稿へ分割 |

- 項目キーは日本語
- 日付書式の初期候補：`YYYY年M月D日` / `YYYY年M月D日（曜）` / `M月D日` / `M/D(曜)` / `YYYY/MM/DD`。候補は「日付書式」シートで管理し、サイトから追加可
- **ランク別テンプレの選択**：セット×ランクを選ぶと、媒体ごとに「そのランク専用」を優先し、なければ「共通」を使う。条件分岐構文は持たない
- 未入力の項目は空で出力する。差し込みがすべて未入力の行は「今回のポイント：」のような見出しごと消す（条件分岐構文の代わり）。空行が3行以上続いたら2行に詰める
- **書類（出力形式 PDF / Docx）**
  - 雛形は `{{ }}` を書き込んだ Google ドキュメント。テンプレの「雛形ファイルID」に指定する
  - 追加・更新時に GAS が雛形の本文（ヘッダー・フッター含む）を読み、テンプレート欄「本文」に写す。画面はこれで入力フォームとプレビューを作る。雛形を直したらテンプレを保存し直す
  - 出力時は画面が `{ '{{締結日:M/D}}': '9/24', ... }` の差し込み表を作って送り、GAS が雛形を複製 → `replaceText` → PDF / Docx に書き出して base64 で返す。値の改行は段落内改行になる
  - 書類では行を消さない（未入力は空欄のまま）。出力画面では書類の内容は確認用で編集不可
- **告知画像（出力形式 画像）**
  - 雛形は `{{ }}` を書き込んだ Google スライド。読み込み時はスライドごとの文字（図形・表・グループ内）を `---` 区切りで「本文」に写す
  - `{{ロゴ}}` は組み込みの差し込み（入力項目ではない）。図形の中に書くと、その図形が団体マスタの「ロゴファイルID」の画像に置き換わる。入力タイプ「画像」の項目も同様（値はドライブの URL か ID）
  - 画面は差し込み表 `replacements` と画像表 `images`（`{ '{{ロゴ}}': ファイルID }`）を送り、GAS が雛形を複製 → 図形を画像に置換 → `replaceAllText` → スライドごとに PNG を書き出して返す。画面でプレビューしてダウンロード
  - 画像として使えるのは `LOGO_FOLDER_ID` のフォルダ内のファイルだけ（利用者がドライブ上の別ファイルを指定して読み出せないようにする）
  - ロゴは団体の新規追加フォーム・管理画面の団体マスタからアップロード（PNG・JPEG・GIF、5MBまで）。既存団体のロゴ変更は編集なので管理者
- **X分割**
  - 基本：`---` で分割
  - 保険：1投稿が上限を超えたら改行・句点の位置で自動分割
  - `(1/3)` 形式の番号付与は選択可
  - 文字数は X 方式（全角2、半角1、URL は一律23）。上限 280（全角140字相当）

## 6. スプレッドシート（12シート）

IDは GAS が自動採番（例 T001）。削除は「有効」列を FALSE。1行目はヘッダー。列はヘッダー名で対応づけるので並べ替えても動く。

**テンプレート**：テンプレID / テンプレ名 / セットID / 媒体ID / ランク（または「共通」）/ 出力形式（テキスト／PDF／Docx／画像。初期はテキストのみ）/ 雛形ファイルID / 有効 / 更新日時

**テンプレート欄**：テンプレID / 欄キー / 内容

**媒体**：媒体ID / 媒体名 / 並び順 / 有効

**媒体欄**：媒体ID / 欄キー / 表示名 / 文字数上限（空欄＝無制限）/ 数え方（通常／X方式）/ 分割ルール（なし／スレッド分割）/ 並び順

**セット**：セットID / セット名 / 説明 / 並び順 / 有効

**ランク**：ランク名 / 並び順 / 有効

**入力項目**：項目キー / 表示名 / 区分（団体／案件）/ 入力タイプ（短文／長文／日付／選択／数値／URL／画像）/ 選択肢（カンマ区切り）/ 書式 / 必須 / 初期値 / 入力例 / 並び順 / 有効

**日付書式**：表示名 / 書式 / 並び順 / 有効

**団体マスタ**：団体ID / （区分＝団体の項目キーが列として並ぶ）/ ロゴファイルID / 有効 / 更新日時
- 区分＝団体の項目を追加したら GAS がこのシートの「ロゴファイルID」の前に列を自動追加

**共通設定**：項目キー / 値 / 説明

**履歴**：履歴ID / 作成日時 / 作成者 / 団体ID / 団体名（作成時点）/ セットID / ランク / 入力値（JSON）/ ステータス（将来の承認用、初期は空欄）

**履歴出力**：履歴ID / 媒体ID / テンプレID / 欄キー / 最終文面 / 修正あり

- 文字列は先頭に `'` を付けて書き込む（電話番号の先頭0消失・日付への自動変換・`=` の数式化を防ぐ）

## 7. GAS 機能（action）

| action | 内容 | 権限 |
| --- | --- | --- |
| init | テンプレ、欄、媒体、媒体欄、セット、ランク、項目、日付書式、共通設定、団体一覧を一括返却 | 利用者 |
| listHistory / getHistory | 履歴一覧・詳細 | 利用者 |
| saveHistory | 生成時の新規保存、修正後の上書き | 利用者 |
| create | 各シートへの新規追加（団体項目なら団体マスタに列追加） | 利用者 |
| verifyAdmin | 管理者パス照合 | 利用者 |
| update | 既存行の更新（団体マスタ含む）。キー列は変更不可 | 管理者 |
| deactivate | 有効を FALSE に（`active: true` で再有効化） | 管理者 |
| deleteHistory | 履歴と履歴出力の行を削除 | 管理者 |
| readDocument | 雛形の Google ドキュメントの本文を返す（テンプレ編集の読み込み） | 利用者 |
| renderDocument | 書類テンプレに差し込み PDF / Docx を、告知画像テンプレに差し込みスライドごとの PNG を base64 で返す | 利用者 |
| uploadImage | ロゴなどの画像を `LOGO_FOLDER_ID` に保存し、ファイルIDを返す | 利用者 |
| readImage | 保存した画像を返す（画面での表示用。`LOGO_FOLDER_ID` 内のみ） | 利用者 |
| setup | 全シートとヘッダーを作成し、空のシートにだけモックデータ投入（GASエディタから手動実行） | — |

## 8. モックデータ

- **実在しない情報のみ**（「サンプル団体」「example.com」「@sample_account」など）。実データは入れない
- 締結告知セット × ランク2種（ゴールド／シルバー）
- 全5媒体、日付書式の切り替え、X分割、ランク別と共通テンプレの混在が確認できる内容
- GAS の `setup()` で投入できる。フロントのモックモードでも同じデータを使う

## 9. 制限・運用メモ

- GAS 同時実行は1ユーザー30（Webアプリは所有者として動くため全員分が合算）。想定規模では問題なし
- 書類・告知画像の出力は Google ドキュメント／スライドを1件複製するので、作成の上限（無料アカウントで各250件／日）にかかる
- GAS のコードを更新して新しい権限（DocumentApp・SlidesApp・DriveApp・UrlFetchApp）を使うようになったら、エディタで一度関数を実行して承認し、Web アプリを「新しいバージョン」で再デプロイする
- GAS はしばらく未使用だと初回応答が数秒遅い → 読み込み中表示を出している

## 10. デプロイ

- `.github/workflows/deploy.yml`：main への push で テスト → ビルド（`GAS_URL` / `GAS_KEY` を埋め込み）→ StatiCrypt 暗号化（`SITE_PASSWORD`）→ 平文漏れ検査 → GitHub Pages
- PR ではシークレットを使わずモックモードのビルドとテストだけを行う
- GAS の URL・キーはリポジトリに直書きしない

### セットアップ手順（作り直し・引き継ぎ時）

**スプレッドシートと GAS**
1. 管理メンバーだけで共有するスプレッドシートを作成し、「拡張機能 → Apps Script」を開く
2. `gas/` の `.gs` ファイルをすべてコピーする（または clasp で `gas/` を push）。`appsscript.json` はプロジェクトの設定で「マニフェスト ファイルをエディタで表示する」をオンにすると編集できる（タイムゾーン Asia/Tokyo・V8 になっていればよい）
3. プロジェクトの設定 → スクリプト プロパティに `USER_KEY`（推測されにくい長いランダム文字列）と `ADMIN_PASSWORD` を追加。必要に応じて `OUTPUT_FOLDER_ID`
4. エディタで `setup` を実行 → 12シートとヘッダー、モックデータができる（既にデータがあるシートには触らない）。初回は権限の承認が出る
5. デプロイ → 新しいデプロイ → ウェブアプリ、実行ユーザー「自分」、アクセス「全員」でデプロイし、URL（末尾 `/exec`）を控える

**GitHub**
1. Settings → Pages → Source を GitHub Actions にする
2. Settings → Secrets and variables → Actions に `GAS_URL`（上の URL）、`GAS_KEY`（`USER_KEY` と同じ値）、`SITE_PASSWORD`（画面を開くときの利用者パスワード）を登録
3. main に push するとデプロイされる

**書類テンプレート**
1. 雛形にする Word ファイルをドライブにアップロードし、開いて「ファイル → Google ドキュメントとして保存」。差し込みたい箇所に `{{項目名}}` を書く
2. 管理画面 → テンプレート → 新規追加で出力形式を PDF か Docx にし、Google ドキュメントの URL を貼って「読み込む」→ 追加

**告知画像テンプレート**
1. スクリプトプロパティ `LOGO_FOLDER_ID` に、ロゴを保存するフォルダ（一般公開しない）の ID を設定
2. Google スライドでデザインを作り、差し込みたい箇所に `{{団体名}}` など、ロゴの位置に置いた図形の中に `{{ロゴ}}` を書く
3. 管理画面 → テンプレート → 新規追加で出力形式を「画像」にし、スライドの URL を貼って「読み込む」→ 追加
4. 団体のロゴは管理画面の団体マスタ（編集）か、団体の新規追加フォームからアップロード

**GAS のコードを更新したとき**：エディタで一度関数を実行して権限を承認し、「デプロイを管理 → 編集 → バージョン：新しいバージョン」で再デプロイする（URL は変わらない）

**実際の GAS につないでローカルで動かす**：リポジトリ直下に `config.local.json`（git 管理外）を置く
```json
{ "gasUrl": "https://script.google.com/macros/s/xxxx/exec", "key": "利用者キー" }
```

## 11. 後回しの機能（データ構造だけ先に用意）

| 機能 | 用意しておくもの |
| --- | --- |
| 承認フロー | 履歴のステータス列 |
| 共通パーツ | 必要になった時点で追加 |

## 12. 進捗

1. ✅ 既存 v0 コードの構成把握 → 方式Bに決定、HTML+JS に移植
2. ✅ GAS：`setup()`（シート作成＋モック投入）、`doPost` のルーティングと認証
3. ✅ フロント：GAS クライアント（通信・認証付与・エラー処理）と init データの保持
4. ✅ テンプレエンジン（単体テストあり）
5. ✅ 生成フロー4ステップ → 出力画面 → 履歴保存
6. ✅ 履歴画面
7. ✅ 管理画面（追加 → 編集・無効化＋管理者パス）
8. ✅ デプロイ設定
9. ✅ 書類（契約書・申込書）の PDF / Docx 出力

10. ✅ GAS への本番デプロイと実データ（告知文面・Orbit 書類）の投入
11. ✅ 告知画像（Google スライド → PNG）と団体ロゴ

未決：画面の見た目の最終調整
