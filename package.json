{
  "name": "partner-generator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/build.mjs --dev",
    "build": "node scripts/build.mjs",
    "test": "node --test test/*.test.mjs",
    "encrypt": "staticrypt dist/*.html -d dist -c false --short --remember 30 --template-title 'FSIF｜発信物ジェネレーター' --template-instructions '利用者パスワードを入力してください' --template-button 'ログイン' --template-placeholder 'パスワード' --template-remember 'ログイン状態を30日間保持' --template-error 'パスワードが違います' --template-color-primary '#1261af' --template-color-secondary '#f6f8fc'",
    "e2e": "node --test --test-concurrency=1 e2e/*.test.mjs"
  },
  "dependencies": {
    "lucide-preact": "^1.48.0",
    "preact": "^10.29.8"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "esbuild": "^0.28.2",
    "playwright": "^1.56.1",
    "staticrypt": "^3.5.4",
    "tailwindcss": "^4.3.3"
  }
}
